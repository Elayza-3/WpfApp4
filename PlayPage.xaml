<Page x:Class="WpfApp4.PlayPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp4"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PlayPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="1.5*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="title" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="40" Grid.ColumnSpan="3"/>
        <TextBlock x:Name="definition" Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Top" FontSize="25" Grid.ColumnSpan="3"/>

        <Button x:Name="one" Grid.Row="3" Grid.Column="1" FontSize="22" Click="first_Click"/>
        <Button x:Name="two" Grid.Row="4" Grid.Column="1" FontSize="22" Click="second_Click"/>
        <Button x:Name="three" Grid.Row="5" Grid.Column="1" FontSize="22" Click="third_Click"/>

    </Grid>
</Page>
